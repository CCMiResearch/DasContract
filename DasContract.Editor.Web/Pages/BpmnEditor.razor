@page "/"
<div class="editor-main">
    <ToolBar ToolBarItems="CreateToolBarItems()"/>
    <div class="@(ShowDetailBar ? "grid" : "") main-container">
        <div id="canvas"></div>
        @if (ShowDetailBar)
        {
            <div class="gutter-col gutter-col-1"></div>
            <ProcessDetailBar />
        }
    </div>
</div>
