@page "/editor"


<div class="@(ShowDetailBar ? "grid" : "") main-container">
    <div id="canvas"></div>
    @if (ShowDetailBar)
    {
        <div class="gutter-col gutter-col-1" @on="ResizeHandler.MainGutterResized"></div>
        <ProcessDetailBar/>
    }
</div>

