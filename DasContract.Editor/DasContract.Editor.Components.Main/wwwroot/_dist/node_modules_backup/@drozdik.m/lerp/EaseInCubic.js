exports.__esModule = true;
exports.EaseInCubic = function (from, to, currentFrame, totalFrames) {
    currentFrame /= totalFrames;
    return (to - from) * currentFrame * currentFrame * currentFrame + from;
};
