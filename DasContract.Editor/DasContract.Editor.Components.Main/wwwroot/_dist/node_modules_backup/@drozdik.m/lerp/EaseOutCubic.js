exports.__esModule = true;
exports.EaseOutCubic = function (from, to, currentFrame, totalFrames) {
    currentFrame /= totalFrames;
    currentFrame--;
    return (to - from) * (currentFrame * currentFrame * currentFrame + 1) + from;
};
