exports.__esModule = true;
exports.EaseOutQuad = function (from, to, currentFrame, totalFrames) {
    currentFrame /= totalFrames;
    return -(to - from) * currentFrame * (currentFrame - 2) + from;
};
