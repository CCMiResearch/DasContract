exports.__esModule = true;
var FetchHelper = /** @class */ (function () {
    function FetchHelper() {
    }
    /**
     * Skips all except an inputted method
     */
    FetchHelper.RespondWithServer = function (fetchRequest) {
        //Pass the fetch request and return server's reponse
        fetchRequest.respondWith(fetch(fetchRequest.request)["catch"](function (error) {
            console.error("Service worker: Fetch to " + fetchRequest.request + " failed:");
            console.error(error);
        }).then(function (fetchResponse) {
            return fetchResponse;
        }));
    };
    return FetchHelper;
}());
exports.FetchHelper = FetchHelper;
