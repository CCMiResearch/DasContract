exports.__esModule = true;
var AjaxSendProgressArgs = /** @class */ (function () {
    //--------------------------------------------------
    //---------CONSTRUCTOR------------------------------
    //--------------------------------------------------
    /**
     * Ajax progress arguments
     * @param computable Is progress computable?
     * @param loaded How much is loaded?
     * @param total How much total will be processed?
     */
    function AjaxSendProgressArgs(computable, loaded, total) {
        if (computable === void 0) { computable = false; }
        if (loaded === void 0) { loaded = -1; }
        if (total === void 0) { total = -1; }
        this.computable = computable;
        this.loaded = loaded;
        this.total = total;
    }
    //--------------------------------------------------
    //----------INFO------------------------------------
    //--------------------------------------------------
    /**
     * Returns true if loaded and total values are awaylable, else false
     * */
    AjaxSendProgressArgs.prototype.Computable = function () {
        return this.computable;
    };
    /**
     * Returns how much has been loaded
     * */
    AjaxSendProgressArgs.prototype.Loaded = function () {
        return this.loaded;
    };
    /**
     * Returns how much total will be loaded
     * */
    AjaxSendProgressArgs.prototype.Total = function () {
        return this.total;
    };
    return AjaxSendProgressArgs;
}());
exports.AjaxSendProgressArgs = AjaxSendProgressArgs;
