exports.__esModule = true;
exports.EaseOutCirc = function (from, to, currentFrame, totalFrames) {
    currentFrame /= totalFrames;
    currentFrame--;
    return (to - from) * Math.sqrt(1 - currentFrame * currentFrame) + from;
};
