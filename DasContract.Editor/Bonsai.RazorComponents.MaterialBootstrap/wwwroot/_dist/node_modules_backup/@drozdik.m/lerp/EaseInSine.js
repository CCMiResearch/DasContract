exports.__esModule = true;
exports.EaseInSine = function (from, to, currentFrame, totalFrames) {
    return -(to - from) * Math.cos(currentFrame / totalFrames * (Math.PI / 2)) + (to - from) + from;
};
