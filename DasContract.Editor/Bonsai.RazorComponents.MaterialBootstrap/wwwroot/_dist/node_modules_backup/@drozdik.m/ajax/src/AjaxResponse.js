exports.__esModule = true;
var AjaxResponse = /** @class */ (function () {
    //--------------------------------------------------
    //---------CONSTRUCTOR------------------------------
    //--------------------------------------------------
    /**
     * Ajax response arguments
     * @param status Status number (200, 404, ...)
     * @param statusText Status text
     * @param response Response text (raw server response)
     */
    function AjaxResponse(status, statusText, response) {
        this.status = status;
        this.statusText = statusText;
        this.response = response;
    }
    //--------------------------------------------------
    //----------STATUS----------------------------------
    //--------------------------------------------------
    /**
     * Returns true if response is an error response, else false
     * */
    AjaxResponse.prototype.IsError = function () {
        //Client error
        if (this.status >= 400 && this.status < 500)
            return true;
        //Server error
        else if (this.status >= 500 && this.status < 600)
            return true;
        //No error
        return false;
    };
    /**
     * Returns status text
     * */
    AjaxResponse.prototype.StatusText = function () {
        return this.statusText;
    };
    /**
     * Returns status number
     * */
    AjaxResponse.prototype.Status = function () {
        return this.status;
    };
    //--------------------------------------------------
    //----------RESPONSE--------------------------------
    //--------------------------------------------------
    /**
     * Returns server response (raw string)
     * */
    AjaxResponse.prototype.Response = function () {
        return this.response;
    };
    return AjaxResponse;
}());
exports.AjaxResponse = AjaxResponse;
